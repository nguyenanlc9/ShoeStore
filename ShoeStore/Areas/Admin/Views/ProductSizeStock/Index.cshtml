@model IEnumerable<ShoeStore.Models.ProductSizeStock>

@{
    ViewData["Title"] = "Quản lý tồn kho sản phẩm theo kích thước";
    ViewData["ActiveMenu"] = "product-size-stock";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="page-inner">
    <div class="page-header">
        <h4 class="page-title">Quản lý tồn kho sản phẩm theo kích thước</h4>
        <ul class="breadcrumbs">
            <li class="nav-home">
                <a asp-area="Admin" asp-controller="Home" asp-action="Index">
                    <i class="fas fa-home"></i>
                </a>
            </li>
            <li class="separator">
                <i class="fas fa-angle-right"></i>
            </li>
            <li class="nav-item">
                <span>Tồn kho sản phẩm</span>
            </li>
        </ul>
    </div>

    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">
                    <div class="d-flex align-items-center">
                        <h4 class="card-title">
                            <i class="fas fa-boxes mr-2"></i>
                            Danh sách tồn kho sản phẩm theo kích thước
                        </h4>
                        <a asp-action="Create" class="btn btn-primary btn-round ml-auto">
                            <i class="fa fa-plus mr-2"></i>
                            Thêm mới
                        </a>
                    </div>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table id="productSizeStockTable" class="display table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>Số lượng tồn kho</th>
                                    <th>Sản phẩm</th>
                                    <th>Kích thước</th>
                                    <th style="width: 120px">Thao tác</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>
                                            <span class="badge badge-success">@item.StockQuantity</span>
                                        </td>
                                        <td>
                                            <h6 class="mb-1">@item.Product.Name</h6>
                                            <span class="text-muted">Mã: @item.Product.ProductId</span>
                                        </td>
                                        <td>
                                            <span class="badge badge-info">@item.Size.SizeValue</span>
                                        </td>
                                        <td>
                                            <div class="form-button-action">
                                                <a asp-action="Edit" asp-route-id="@item.ProductSizeStockID"
                                                   class="btn btn-link btn-primary" data-toggle="tooltip"
                                                   data-original-title="Sửa">
                                                    <i class="fa fa-edit"></i>
                                                </a>
                                                <a asp-action="Details" asp-route-id="@item.ProductSizeStockID"
                                                   class="btn btn-link btn-info" data-toggle="tooltip"
                                                   data-original-title="Chi tiết">
                                                    <i class="fa fa-info-circle"></i>
                                                </a>
                                                <a asp-action="Delete" asp-route-id="@item.ProductSizeStockID"
                                                   class="btn btn-link btn-danger" data-toggle="tooltip"
                                                   data-original-title="Xóa"
                                                   onclick="return confirm('Bạn có chắc muốn xóa không?');">
                                                    <i class="fa fa-trash"></i>
                                                </a>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            $('#productSizeStockTable').DataTable({
                "pageLength": 10,
                "order": [[1, "asc"]],
                "language": {
                    "lengthMenu": "Hiển thị _MENU_ mục",
                    "zeroRecords": "Không tìm thấy dữ liệu",
                    "info": "Hiển thị trang _PAGE_ của _PAGES_",
                    "infoEmpty": "Không có dữ liệu",
                    "infoFiltered": "(lọc từ _MAX_ mục)",
                    "search": "Tìm kiếm:",
                    "paginate": {
                        "first": "Đầu",
                        "last": "Cuối",
                        "next": "Sau",
                        "previous": "Trước"
                    }
                }
            });

            $('[data-toggle="tooltip"]').tooltip();
        });
    </script>
}

@section Styles {
    <style>
        .badge {
            font-size: 0.9em;
            padding: 5px 10px;
        }

        .form-button-action {
            display: flex;
            gap: 5px;
        }

        .btn-link {
            padding: 5px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            border-radius: 50%;
            width: 35px;
            height: 35px;
        }

            .btn-link i {
                font-size: 16px;
                color: white;
            }

        .table td {
            vertical-align: middle;
        }
    </style>
}
